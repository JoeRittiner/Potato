version: "3.9"

name: discord

services:
  update:
    build: .
    env_file: .env
    command: node src/deploy-commands.js

  bot:
    build: .
    restart: unless-stopped
    env_file: .env
    command: npm run start